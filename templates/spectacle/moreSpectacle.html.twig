{% for seance in showDate |slice(5,100) %}
    <div class="col-12 col-md-8 p-4">
        <p class="description">{{ seance.dateShow | date('d.m.Y à H:i') }}</p>
    </div>
    <div class="col-12 col-md-3">
        <a href="https://54yzsyel.mapado.com/en/"><button class="calendarButton">
                commander
            </button></a>
    </div>
{% endfor %}