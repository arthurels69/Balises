{{ form_start(form) }}
    {{ form_row(form.email, {'label': 'Adresse email', 'attr': {'placeholder': 'Adresse email ...' }}) }}
    {{ form_row(form.password, {'label': 'Mot de passe', 'attr': {'placeholder': 'Mot de passe ...' }}) }}
    {{ form_row(form.confirm_password, {'label': 'Confirmation du mot de passe', 'attr': {'placeholder': 'Répetez votre mot de passe...' }}) }}
    <div class="text-center">
    <button class="btn btn-dark btn_save_form">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
