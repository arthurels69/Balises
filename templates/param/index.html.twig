{% extends 'basePage.html.twig' %}

{% block title %}Param index{% endblock %}

{% block body %}
    <div class="container bg-custom">
        <h1>Coefficients Balise</h1>

        <div class="col-sm-2">

            <a type="button" href="{{ path('user_index') }}"
               data-toggle="tooltip" data-placement="top" title="Retour"
               class="btn btn-secondary mt-2 ml-2 mb-2"><i class="fas fa-reply"></i>
            </a>
        </div>
        <table class="table">
            <thead>
                <tr>
{#                    <th>Id</th>#}
                    <th>Coeff. Revente</th>
                    <th>Coeff. Cession</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for param in params %}
                <tr>
{#                    <td>{{ param.id }}</td>#}
                    <td>{{ param.resaleCoeff }}</td>
                    <td>{{ param.redistributedCoeff }}</td>
                    <td>
                        <a href="{{ path('param_show', {'id': param.id}) }}"data-toggle="tooltip" data-placement="top"  title="Détail"><i class="fas fa-file-alt" style = "color : maroon"></i></a>
                        <a href="{{ path('param_edit', {'id': param.id}) }}"data-toggle="tooltip" data-placement="top"  title="Modification"><i class="fas fa-edit" style = "color : maroon"></i></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">Aucun Enregistrement</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

{#        <a href="{{ path('param_new') }}">Create new</a>#}

    </div>
{% endblock %}
