{% extends is_granted('ROLE_ADMIN')
    ? 'base_admin.html.twig'
    : 'baseAdminTheater.html.twig' %}

{% block title %}Représentation{% endblock %}

{% block body %}
    <div class="card justify-content-left p-0 w-100">
        <h1>Suppression Représentation</h1>

        <table class="table">
            <tbody>
            {# <tr>
                 <th>Id</th>
                 <td>{{ show_date.id }}</td>
             </tr>#}
            <tr>
                <th>Représentation du</th>
                <td>{{ show_date.dateShow ? show_date.dateShow|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            </tbody>
        </table>
        <div class="row justify-content-center">
            {{ include('show_date/_delete_form.html.twig') }}
        </div>
        <div class="row justify-content-center">
            {#    <a href="{{ path('show_date_index') }}">Retour</a>#}
            {#    <a href="javascript:history.go(-1)">Retour</a>#}
            {#        <form> <input type ="button" class="btn btn-secondary" value="Retour" onclick="history.go(-1)"</form>#}
            <a type="button" href="javascript:history.go(-1)"
               data-toggle="tooltip" data-placement="top" title="Retour"
               class="btn btn-secondary mb-2"><i class="fas fa-reply"></i>
            </a>
        </div>

        {#    <a href="{{ path('show_date_edit', {'id': show_date.id}) }}">edit</a>#}

    </div>
{% endblock %}
